# Copy this file to a new file named ".env" and paste your REAL Neon
# connection string there. Never put the real value here, and never
# commit the real ".env" file - only this template is safe for GitHub.

DATABASE_URL=postgresql://<user>:<password>@<host>/<dbname>?sslmode=require
